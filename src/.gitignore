# =============================================================================
# Generated by ninit from shared gitignore source files.
# Put project-specific ignore rules only in the user-managed section below.
# To disable a generated rule, comment it out in place instead of deleting it.
# Avoid adding new lines inside generated source blocks to minimize merge conflicts.
# =============================================================================

# >>> NINIT USER SECTION START
# Add project-specific ignore rules in this section only.
# If you need to disable a generated rule below, comment it out in place.
# <<< NINIT USER SECTION END

# =============================================================================
# Source: github/gitignore/Firebase.gitignore
# Managed from the source file above.
# Comment out rules here if needed, but do not add or delete lines in this block.
# =============================================================================
# Firebase build and deployment files
/firebase-debug.log
/firebase-debug.*.log
.firebaserc

# Firebase Hosting
/firebase.json
*.cache
hosting/.cache

# Firebase Functions
/functions/node_modules/
/functions/.env
/functions/package-lock.json

# Firebase Emulators
/firebase-*.zip
/.firebase/
/emulator-ui/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (local configs)
/.env.*
